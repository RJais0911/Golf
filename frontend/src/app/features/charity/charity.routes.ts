import { Routes } from '@angular/router';
import { CharitySelectComponent } from './charity-select/charity-select.component';

const routes: Routes = [{ path: '', component: CharitySelectComponent }];

export default routes;
